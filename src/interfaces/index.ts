import { MovieCreate, MovieList, MovieUpdate, MovieRepo } from './movie.interfaces';
import { Pagination, PaginationParams } from './pagination.interfaces';

export { MovieCreate, MovieList, MovieUpdate, MovieRepo, Pagination, PaginationParams };
